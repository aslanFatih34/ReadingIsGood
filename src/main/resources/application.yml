spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/getir?currentSchema=readingisgood
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  security:
    user:
      name: fatih
      password: aslan
      roles: admin

